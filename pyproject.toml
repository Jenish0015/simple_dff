[project]
name = "simple-dff"
version = "0.1.0"
description = "Complete the implementation in sources/dff.sv."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "cocotb",
    "pytest",
]
